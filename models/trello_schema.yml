version: 2

sources:
  - name: trello_data
    schema: "{{ env_var('DBT_TARGET_SCHEMA') }}"
    tables:
      - name: actions
      - name: boards 
      - name: cards 
      - name: checklists 
      - name: lists 
      - name: users  

       
